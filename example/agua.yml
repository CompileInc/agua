test: test.csv
config:
  name:
    comparator: string_similarity
    kwargs: {'min_score': 90}
    test_column: final_name
  age:
    comparator: approximate
    kwargs: {'delta': 0.1}
  fruit:
    comparator: exact
    separator: '\n'
